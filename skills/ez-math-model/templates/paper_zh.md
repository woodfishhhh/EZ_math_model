# {题目标题}

> **使用说明**：本模板由 ez-math-model 在 zhanwen 上游模板不可用时使用。
> 每章末尾的 `> 写作指引` 是对 writer 的提示，落盘前必须**全部删除**。
> 模板字数指引来自 MathModelAgent 的章节模板与 zhanwen 优秀论文统计。
> 正式论文不得残留 `{...}` 占位符，也不得出现 `runtime/`、`output/`、
> `summary.json`、`execution_log`、manifest 或调试日志等工程痕迹。

**摘要**

第一段：背景。约 100 字，介绍研究问题的实际背景、当前面临的挑战、本文的
研究方向。

针对问题一，选用了 {模型名}。具体做法是 {建模思路}，通过 {求解方法} 得到
{核心数值结果}。该结果表明 {简短结论}。

针对问题二，选用了 {模型名}。具体做法是 {建模思路}，通过 {求解方法} 得到
{核心数值结果}。该结果表明 {简短结论}。

针对问题三，选用了 {模型名}。具体做法是 {建模思路}，通过 {求解方法} 得到
{核心数值结果}。该结果表明 {简短结论}。

最后通过对 {关键参数} 进行 ±20% 范围的敏感性分析，发现 {敏感性结论}，
表明所建模型在合理参数变化范围内具有 {鲁棒性 / 稳定性} 评价。综合来看，
本文建立的 {模型框架} 能够 {解决问题}。

**关键词**：{关键词1}；{关键词2}；{关键词3}；{关键词4}；{关键词5}

> 写作指引：摘要总篇幅 600-700 字，**绝不超过一页纸**。每个问题段约 170 字，
> 必须含具体数值（精度、误差、运行时间、最优值等）。关键词 4-5 个，无编号，
> 优先选模型名 + 关键算法 + 题目领域名词。

---

# 一、问题重述

## 1.1 问题背景

约 160 字一段，结合题目原文与领域常识，说明研究对象的实际意义、当前面临
的挑战以及本文研究方向。如有可能，引用 1-2 篇综述性文献交代行业现状。

## 1.2 问题重述

本文基于以上信息建立数学模型来解决以下问题：

- 问题一：{原题问题一的概括}
- 问题二：{原题问题二的概括}
- 问题三：{原题问题三的概括}

> 写作指引：1.1 与 1.2 合计 8-12% 篇幅。1.2 用项目符号是允许的（属于"复述
> 题目要求"，不算正文论述）。绝不照抄原题，要用自己的话浓缩。

---

# 二、问题分析

## 2.1 问题一的分析

第一段（约 130 字）：本题要求 {目标}，考虑 {约束}，将问题归入 {模型类别}
（预测 / 评价 / 分类 / 优化）。说明问题的难点与关键点。

第二段（约 120 字）：综合数据特征与建模目标，初步选用 {模型名} 作为基础
方案，相较 {备选模型} 的优势在于 {对比理由}。后续将通过 {验证策略} 进一步
检验模型适配性。

## 2.2 问题二的分析

（同上模板，每个问题约 250 字，分两段）

## 2.N 问题 N 的分析

> 写作指引：每问 250 字左右，必须分两段：第一段定性"是什么类型的问题"，
> 第二段定性"为什么选这个模型"。**禁止**仅写一句"本题为预测问题"。

---

# 三、模型假设

(1) 假设 1：{假设内容}。
合理性说明：{结合领域知识，论证为什么这样假设是合理的}。

(2) 假设 2：{假设内容}。
合理性说明：{...}。

(3) 假设 3：{假设内容}。
合理性说明：{...}。

(4) 假设 4：{假设内容}。
合理性说明：{...}。

> 写作指引：3-4 条假设。每条必须**附 Justification**（"假设是合理的，
> 因为..."）。**绝不**写"假设数据准确"这种空假设。常见高质量假设：
> 简化时间维度、忽略次要因素、参数稳定性、独立性。

---

# 四、符号说明与数据预处理

## 4.1 符号说明

| 符号 | 含义 | 单位 |
| --- | --- | --- |
| $x$ | {含义} | {单位} |
| $y$ | {含义} | {单位} |
| $\alpha$ | {含义} | {单位} |
| $\beta$ | {含义} | {单位} |

> 写作指引：列出所有出现的固定量与变量。**一个符号只表示一个变量**。
> 优先用 LaTeX 数学符号；表格需含"单位"列，无单位写"-"。

## 4.2 数据预处理

> **数据驱动题**版本（约 200 字）：
> 数据集共 N 条记录，M 个特征。通过 EDA 发现：缺失率 {x}%（采用 {填充策略}），
> 异常值 {y}%（按 IQR 准则识别），各特征分布详见图 X。变量间相关性分析显示
> {主要发现}，提示后续建模需考虑 {共线性 / 标准化 / 特征选择}。
>
> **物理 / 力学机理题**版本（约 150 字）：
> 题目给定常量 H = {value} mm、m = {value} kg、α = {value} rad 等。经几何
> 一致性检查（{量纲推导}）与物理可行性验证，发现 {结论}。本节**不**进行
> 描述性统计，理由是参数为题给定常量而非样本分布。

> 写作指引：必须按题目类型二选一，**绝不**对物理常量做"异常值清洗"——
> 评委会扣模板化分。

---

# 五、模型的建立与求解

## 5.1 问题一模型的建立与求解

### 5.1.1 模型的建立

{模型简介，约 100 字。包含：模型名、为什么选、与备选方案的对比}。

模型形式：

$$
\min_{x \in \mathcal{X}} \; f(x) = \sum_{i=1}^{N} c_i x_i \quad
\text{s.t.} \; g_j(x) \le 0,\, j = 1, \dots, M
$$

公式中各参数来源：$c_i$ 由 {数据来源} 估计；$g_j$ 由 {物理 / 题目要求}
给出。**每个参数都必须写来源**。

### 5.1.2 模型的求解

求解步骤（约 150 字）：{步骤一} → {步骤二} → {步骤三}。

为避免过早收敛 / 数据泄露 / 物理不可行，引入 {对应保护机制}。

结果如下表：

| 指标 | 数值 |
| --- | --- |
| {指标 1（如 R²）} | {值} |
| {指标 2（如 RMSE）} | {值} |
| {指标 3（如运行时间）} | {值} |

![问题一关键结果图](fig_q1_main.png)

图片解读 1（约 40 字）：{从 print 输出解读趋势，引数据特征中的具体数值}。
图片解读 2（约 40 字）：{从 print 输出解读关键拐点 / 极值 / 异常区段}。
图片解读 3（约 60 字）：{结合建模思路解释为什么会出现该现象，呼应假设与
理论支撑}。

**对优化类问题，必须再写一段（约 80 字）**：
"无约束最优解为 {value}，但该解违反 {几何 / 物理 / 题目要求} 中的
{具体约束}（如绳长超出模型高度），因此引入约束 {表达式}，约束下的最优解
为 {value}，{影响分析}。"

## 5.2 问题二模型的建立与求解

（同上结构，每个问题约 600 字）

## 5.N 问题 N 模型的建立与求解

> 写作指引：本章占全文 50-60%。每问标准结构 = 5.X.1 模型建立（公式 +
> 参数来源）+ 5.X.2 求解（步骤 + 表格 + 至少 1 张图 + 3 行解读）。
> 公式块单独成段，**禁止**塞进段落。表格用三线表格式（markdown 表格即可）。

---

# 六、敏感性分析

针对关键参数 ${\theta_1, \theta_2, \theta_3}$ 在 ±20% 范围内做敏感性
分析，评估指标为 {R² / RMSE / 目标函数值 / 排序变化}。具体扰动方式：
{固定其他参数仅扰动一个 / 拉丁超立方采样 / Sobol 全局敏感性}。

![敏感性分析](fig_sensitivity.png)

图片解读（约 100 字）：{说明哪个参数最敏感，敏感程度的量化描述}；
{说明其他参数对结果的影响幅度}；{结合模型给出鲁棒性评价}。

> 写作指引：本章 4-8% 篇幅。**禁止**仅写"模型表现稳定"这种空话，必须给
> 数值变化区间（如"参数 α 变动 ±20% 时，R² 在 [0.81, 0.86] 内波动"）。

---

# 七、模型的评价、改进与推广

## 7.1 模型的优点

第一，{优点 1，具体依据}。第二，{优点 2，具体依据}。第三，{优点 3，具体
依据}。第四，{优点 4，具体依据}。

## 7.2 模型的缺点

第一，{缺点 1，具体依据}。第二，{缺点 2，具体依据}。

## 7.3 模型的改进与推广

{推广到其他场景的可能性，每条都说明前提条件}。例如：本模型可推广到 {场景
A}，前提是 {假设 X 成立}；亦可结合 {方法 B} 进一步提升 {指标}。

> 写作指引：4-8% 篇幅。**优点数量必须多于缺点**（典型 4 优 2 缺）。每条
> 优缺点附具体证据（数值 / 对比实验 / 文献支持）。

---

# 参考文献

{[^1] 完整参考文献信息，GB/T 7714 格式}

{[^2] 完整参考文献信息}

{[^3] 完整参考文献信息}

> 写作指引：每条 `[^N]` 全文唯一引用一次。理论性章节（模型选择理由、敏感性
> 分析方法论、模型评价依据）必须有引用。常见类别：经典教材（AHP / TOPSIS /
> 蒙特卡洛）+ 近 5 年综述 + 题目领域期刊 1-2 篇。

---

# 附录

## 附录 A：核心代码

{从 src/ 复制关键脚本片段，**不要**复制全部，重点是模型核心逻辑与求解
代码，约 100-300 行}。

## 附录 B：补充数据表

{若 results/ 中有大表（≥ 50 行），放这里；正文只放摘要表}。

> 写作指引：附录不计入正文字数。代码必须可运行，**不要**贴出错代码。
> 数据表用三线表格式，列名清晰带单位。
